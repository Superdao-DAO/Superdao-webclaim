/**
 * This is the main promissory token contract class
 */
export class Contract {
  /**
   * This is the class constructor
   * @constructor
   * @param {string} address - The contract address to use.
   */
  constructor(address) {
    this.address = address;
  }
}
